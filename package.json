{
  "name": "sentryprime-v2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "build:common": "pnpm --filter @common build",
    "dev": "pnpm -r dev",
    "start": "pnpm -r start"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}

