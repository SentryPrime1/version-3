{
  "name": "sentryprime-v2",
  "version": "1.0.0",
  "description": "SentryPrime v2 - Enterprise Security Scanning Platform",
  "packageManager": "pnpm@10.4.1",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "start": "pnpm -r start",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "start:prod": "pnpm --filter backend run start:prod"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}

