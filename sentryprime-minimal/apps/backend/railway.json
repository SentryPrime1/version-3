{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd /app && pnpm install --no-frozen-lockfile && cd packages/common && pnpm build && cd ../../apps/backend && pnpm build"
  },
  "deploy": {
    "startCommand": "cd /app/apps/backend && node dist/main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
