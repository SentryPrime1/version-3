{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install -g pnpm && pnpm install --no-frozen-lockfile && cd packages/common && pnpm build && cd ../../apps/backend && pnpm build"
  },
  "deploy": {
    "startCommand": "cd apps/backend && node dist/main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
